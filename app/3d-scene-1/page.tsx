import EcoGardenApp from "@/components/EcoGardenApp";

/** Scene 1 of the 3D world: the early version where every action grows a tree. */
export default function ThreeDSceneOnePage() {
  return (
    <EcoGardenApp
      basic
      badge="3D · Scene 1"
      subtitle="The first 3D world — every action you log grows a tree in a living, breathing scene. Trees sway, grow, and wilt as your garden's health changes."
    />
  );
}
